{
  "name": "root-proxy",
  "private": true,
  "scripts": {
    "install": "cd frontend && npm install",
    "build": "cd frontend && npm run build && cp -r public ../public"
  },
  "engines": {
    "node": "20.x"
  }
}
